# 排行榜管理页面接入审计验收

日期：2026-09-22

## 审计

- 后端正式接口为 `GET /admin/v1/leaderboards/level?page=N`，复用 `player:read` 权限并固定每页 20 条。
- 管理后台只查看全平台榜单，不读取或保存微信好友关系。
- V1 只开发闯关榜，挑战榜只保留产品入口。

## 执行

- 新增一级菜单“排行榜”和受权路由 `/leaderboards`。
- 页面提供“闯关榜 / 挑战榜”页签。
- 闯关榜展示排名、头像、昵称、用户 ID、平台、当前关卡、达到时间、更新时间和用户详情入口。
- 闯关榜固定每页 20 条，使用上一页、下一页分页。
- 挑战榜显示“暂未开放”，切换页签不会调用未实现接口。

## 自动验收

- API 契约测试覆盖页码请求与响应解析。
- `npm test`：8 个测试文件、14 项测试全部通过。
- `npm run lint`：通过。
- `npm run build`：`vue-tsc -b` 类型检查及 Vite 生产构建通过。
- `git diff --check`：通过。

## 云端验收

- 需要先部署后端排行榜版本并创建、回填 `level_leaderboard` 集合。
- 本地构建通过不等于云端接口已部署。

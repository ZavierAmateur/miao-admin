# A6F 用户详情存档内容回显验收报告

## 验收日期

2026-09-20

## 验收内容

- 用户详情对拥有 `save:read` 权限且存在云存档的用户加载当前 `user` 存档。
- 核心游戏进度使用中文名称直接回显。
- 其他字段和原始 JSON 默认折叠，页面保持简洁。
- 存档展示只读，未增加编辑入口。
- 存档读取错误与基础详情错误相互隔离。

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：5 个测试文件、8 项测试通过。
- `npm run build`：通过。
- `git diff --check`：通过。

## 线上验收

使用 `npm run dev:cloud` 登录管理后台，进入有云存档的用户详情，确认核心字段、更多字段及原始 JSON 与存档诊断接口返回的 `current.user` 一致。

本轮浏览器渲染验收到达管理员登录页，因不读取或代填管理员密码，登录后的真实数据页面由用户完成最终目视确认。

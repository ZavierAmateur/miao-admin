# A6D CloudBase 冷启动登录提示验收报告

## 验收日期

2026-09-20

## 验收内容

- 线上健康接口返回 HTTP 200。
- 线上管理员会话接口在未登录时返回预期 HTTP 401，而不是 5xx。
- 新增自动测试证明：第一次连接失败后 `sessionChecked=false`，第二次连接恢复可正常取得管理员身份。
- 登录页可自动复检并清理陈旧的 `unavailable=1` 提示，也可由管理员手动点击“重试连接”。
- 错误凭据与服务不可用使用不同提示状态。

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：3 个测试文件、5 项测试通过。
- `npm run build`：通过。
- `git diff --check`：通过。

结论：CloudBase 服务恢复后，后台不再被首次冷启动错误锁死；陈旧错误提示能够自动清理，也可以手动重试。

# A3 存档回滚与封禁验收报告

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：2 个测试文件、3 项测试全部通过。
- `npm run build`：通过。
- `git diff --check`：通过。

## 已实现页面

- 玩家详情增加存档诊断、封禁和解封入口。
- 存档诊断页展示当前/上一版本、大小和 `user` 字段差异；内部 hash 已在 A3B 正式联调时从运营页面移除。
- 超管可填写原因回滚上一版；请求携带当前 expectedRevision。
- 封禁对话框支持临时/永久、到期时间、原因和内部备注；运营角色不能选择永久封禁。
- 接口文档同步新增四个 A3 请求。

## 验收边界

- 本地内存服务没有可从浏览器直接操作的游戏测试玩家；有数据、回滚、封禁和权限场景由后端 70 项自动测试覆盖。
- 未在生产 CloudBase 或真实玩家上执行写操作；隔离环境真实联调后才能标记 A3 生产完成。

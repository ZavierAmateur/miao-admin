# A4 精简错误日志本地验收报告

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：3 个测试文件、4 项测试全部通过。
- `npm run build`：通过。
- `git diff --check`：通过。

## 已实现页面

- 侧边栏新增按权限显示的“错误日志”入口。
- 支持 requestId、业务错误码、开始/结束时间组合筛选和游标分页。
- 列表展示脱敏摘要；点击“详情”通过日志 ID 获取脱敏详情。
- 登录页说明已同步当前 V1 能力，不再展示未接入的远程配置占位描述。

## 页面联调验收

- 本地内存后端生成受控 `INVALID_CURSOR` 错误，列表正确显示时间、错误码、HTTP 400、接口模板和 requestId。
- 输入完整 requestId 后精确命中 1 条记录。
- 点击“详情”成功读取安全中文提示、异常类型和日志 ID。
- 页面未展示请求体、Cookie、管理员密码、Token、玩家身份、存档正文或堆栈。
- 退出登录成功；临时日志随内存后端退出清除。

## 验收边界

- 当前后台已经通过类型检查、静态检查、单元测试、生产构建和本地页面联调。
- 后端部署后的 `npm run dev:cloud` 真实页面验收已经完成，结果见《A4-精简错误日志CloudBase验收报告》。“云端待部署”边界已经解除。
- 本阶段不包含错误处置状态、负责人、备注、删除、导出、告警和监控指标。

# A1 管理员认证接入审计

- 角色收敛为 `admin/operator`，删除前端 `viewer/support` 类型。
- 登录、会话恢复和退出仅调用 `/admin/v1/auth/*`。
- API Client 使用 `credentials: include`，不读取或保存管理 token。
- 未认证路由先恢复服务端会话，失败后回到登录页。
- 登录后的跳转地址只接受站内 `/` 开头路径。
- 本地开发通过 Vite 同源代理访问后端，生产通过 `VITE_ADMIN_API_BASE_URL` 配置。
